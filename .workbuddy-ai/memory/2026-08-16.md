# 2026-08-16

## 任务：把图片还原的"AI剧本创作·信息确认页"部署到 GitHub

- 工作区：`E:/workbuddy/数据/2026-08-16-18-34-58/`
- 产物：`index.html` + `styles.css` + `script.js`（含可交互 demo）
- 仓库：`https://github.com/13912952268gujian-blip/ai-script-studio`（public，分支 `master`）
- Pages：`https://13912952268gujian-blip.github.io/ai-script-studio/`（build_type=legacy，正在 building 中，预计 1-2 分钟上线）

## 经验

- `gh` CLI 已登录 13912952268gujian-blip，scope 含 `repo` + `workflow`，可直接 CLI 操作 GitHub。
- 使用 `gh api POST /repos/:owner/:repo/pages -f 'source[branch]=master' -f 'source[path]=/'` 启用 Pages。
- **GitHub Pages 不能用于 private 仓库**（free plan），遇到 422 时改 `gh repo edit --visibility public --accept-visibility-change-consequences` 再启。
- `gh repo create --source=. --push` 一条命令搞定「创建 + 推送」，但前提是先本地 `git init` + `git commit`。
- GitHub MCP 集成（`mcp__github__create_repository`）返回 403 — 当前权限不够创建仓库，仓库管理走 `gh` CLI 更稳。
